import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 10,       
    },
    text: {
        fontSize: 40,
        fontWeight: 'bold',
        color: 'chocolate',
    },
    image: {
        width: '100%',
        resizeMode: 'cover',
    },
});
export default styles;